services:
  mysql:
    image: mysql:8.0
    container_name: wrg-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: weekly_report_db
    ports:
      - "3306:3306"
    volumes:
      - wrg_mysql_data:/var/lib/mysql

volumes:
  wrg_mysql_data:
